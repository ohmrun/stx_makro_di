package stx.makro.di.test.head.data.some_package;

class TestFromSomePackage{
  public function new(){
    // new Pkg(
    //   function(){}
    // );
  }
  public function run(){
    //var a = Assembly.here();
    //trace(a);
    //trace(a.isSubmodule());
    //trace(a.getPackage());
  }
  //public function act(v:Map){
    /*
    var di = new DI();
        di.add(function():String{ return "hello";});
        di.add(function():Int{ return 123;});
    v.set(Assembly.here(),di);*/
  //}
}